-   config:
        name: 用户信息
        request:
            base_url: 'http://47.100.182.135/api/v4'
            headers:
                content-type: application/x-www-form-urlencoded
                token: c20aa28c8be44eed8ba5225767ef100a
        variables: []
-   test:
        name: case01.正向获取用户信息
        request:
            method: GET
            params:
                user_id: '93'
            url: /user/get
        validate:
        -   eq: [status_code, 200]
        -   eq: [content.message, ok]
-   test:
        name: case02.获取用户信息-传空参数
        request:
            method: GET
            url: /user/get
        validate:
        -   eq: [status_code, 200]
        -   eq: [content.code, 400]  # 400
        -   eq: [content.message, "用户ID不能为空！"]
-   test:
        name: case03.获取用户信息-传特殊字符
        request:
            method: GET
            params:
                user_id: '@'
            url: /user/get
        validate:
        -   eq: [status_code, 200]
        -   eq: [content.code, 400]  # 400
        -   eq: [content.message, "获取失败"]
